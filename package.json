{
  "scripts": {
    "gen-api": "elm-open-api ./openapi.yml --module-name=Api ",
    "dev": "elm-land server",
    "review": "elm-review --watch --fix"
  },
  "devDependencies": {
    "elm-land": "^0.20.0",
    "elm-open-api": "^0.2.2",
    "elm-review": "^2.11.1"
  }
}
