{
  "scripts": {
    "gen-api": "elm-open-api ./openapi.yml --module-name=Api ",
    "dev": "elm-land server",
    "review": "elm-review --watch --fix"
  },
  "devDependencies": {
    "elm-land": "^0.19.4",
    "elm-open-api": "^0.1.0",
    "elm-review": "^2.10.3"
  }
}
